# 实现计划

- [x] 1. 创建环境变量配置模块
  - 在 `src/lib/env.js` 中创建环境变量加载和验证函数
  - 实现 `loadEnvConfig()` 函数来读取 `VITE_LLM_API_KEY`
  - 实现 `getApiKey()` 函数作为获取 API 密钥的接口
  - 添加错误处理，当环境变量未设置时抛出有意义的错误
  - _需求: 1.1, 1.2_

- [x] 2. 更新 Webpack 配置以支持环境变量
  - 在 `webpack.config.js` 中添加 `dotenv` 包的加载
  - 配置 `DefinePlugin` 将 `VITE_LLM_API_KEY` 注入到构建中
  - 确保环境变量在开发和生产环境中都可用
  - _需求: 2.1, 2.2_

- [x] 3. 更新 LLM 配置以使用环境变量
  - 在 `src/legacyApp.js` 中导入环境变量配置模块
  - 将 `LLM_CONFIG` 中的硬编码 `apiKey: "***"` 替换为从环境变量读取的值
  - 添加启动时的配置验证
  - _需求: 1.1, 1.3_

- [x] 4. 添加错误处理和用户反馈
  - 在应用启动时验证 API 密钥是否已配置
  - 当 API 密钥缺失时，在 UI 中显示清晰的错误消息
  - 防止在 API 密钥缺失时进行 LLM 调用
  - _需求: 1.2, 1.4_

- [x]* 5. 创建环境变量配置文档
  - 编写 `.env.example` 文件作为开发配置模板
  - 在 README 中添加环境变量配置说明
  - 提供开发和生产环境的配置示例
  - _需求: 2.1, 2.2_

- [x]* 6. 编写单元测试
  - 测试环境变量加载功能
  - 测试缺失 API 密钥时的错误处理
  - 测试配置验证逻辑
  - _需求: 1.1, 1.2_
